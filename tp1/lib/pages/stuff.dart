import 'package:flutter/material.dart';

class PageStuff extends StatelessWidget {
  const PageStuff({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Page 3'));
  }
}
